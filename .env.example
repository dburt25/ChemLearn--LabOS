# Copy this file to `.env` and fill in your Docker Hub credentials
# so the docker-scout helper container can authenticate non-interactively.
DOCKER_HUB_USER=your-docker-id
DOCKER_HUB_PAT=your-personal-access-token
