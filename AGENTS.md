# Agent Instructions

## Commands
- Setup: `make setup`
- Run demo pipeline: `make run-demo`
- Run tests: `make test`
- Lint: `make lint`

## Notes
- Use Python 3.11+.
- COLMAP is optional for Phase 0; without it, the pipeline should fail with actionable guidance after writing reports.
